import { createContext } from "react";

export const Productss = createContext();
export const ProductProvider = Productss.Provider;  //if you go to network tab click any product se after press backspace main api will not call